# Task 7.4 - دليل استخدام نموذج المنتج (UX Guide)

## 📋 نظرة عامة

تم إنشاء نظام شامل لإدارة المنتجات يتضمن:
- **صفحة إنشاء منتج جديد** (`/dashboard/products/new`)
- **صفحة تعديل منتج موجود** (`/dashboard/products/[id]/edit`)
- **نموذج موحد قابل لإعادة الاستخدام** (`ProductForm`)

---

## 🎯 التجربة المستخدم الذكية (Smart UX)

### 1. **تصميم النموذج (Form Design)**

#### ✅ المبادئ التصميمية:
- **تجميع منطقي للحقول:** الحقول مرتبة حسب الأهمية والمنطق (معلومات أساسية → تصنيف → سعر → مخزون)
- **إرشادات واضحة:** كل حقل يحتوي على:
  - تسمية واضحة بالفرنسية
  - علامة `*` للحقول الإجبارية
  - وصف مساعد (مثل "En dirhams (MAD)" لسعر الشراء)
- **تغذية راجعة فورية:** الأخطاء تظهر مباشرة تحت الحقل المتأثر

#### 📐 التخطيط (Layout):
```
┌─────────────────────────────────────────┐
│        [عنوان الصفحة - مركز]            │
├─────────────────────────────────────────┤
│  ┌──────────────────────────────────┐   │
│  │  *Nom du produit                 │   │
│  │  [_________________________]     │   │
│  │  ⚠️ Erreur (si applicable)       │   │
│  └──────────────────────────────────┘   │
│                                         │
│  ┌──────────────────┬─────────────────┐│
│  │ *Marque          │  Catégorie      ││
│  │ [Dropdown]       │  [Dropdown]     ││
│  └──────────────────┴─────────────────┘│
│                                         │
│  ...                                    │
│                                         │
│  ┌──────────────────────────────────┐   │
│  │  [Annuler]    [Créer/Enregistrer] │   │
│  └──────────────────────────────────┘   │
└─────────────────────────────────────────┘
```

---

### 2. **تدرج الفئات الذكي (Smart Category Cascade)**

#### 🎯 كيف يعمل:
1. المستخدم يختار **Catégorie** (اختياري)
2. النظام يفلتر **Sous-catégorie** تلقائياً لعرض فقط السُب-فئات التابعة للفئة المختارة
3. عند تغيير الفئة، يتم إعادة تعيين السُب-فئة تلقائياً

#### 💡 الفائدة للمستخدم:
- **تقليل الأخطاء:** لا يمكن اختيار سُب-فئة غير مرتبطة بالفئة
- **واجهة نظيفة:** فقط الخيارات الصحيحة تظهر
- **تجربة سلسة:** لا حاجة للبحث في قائمة طويلة

#### 🔄 مثال عملي:
```
الخطوة 1: اختيار "Électronique" → Catégorie
         ↓
الخطوة 2: القائمة تتغير تلقائياً لعرض:
          - Téléviseurs
          - Ordinateurs
          - Téléphones
          (بدون باقي السُب-فئات)
```

---

### 3. **التحقق من الصحة (Validation)**

#### 🛡️ مستويان من التحقق:

**أ) التحقق من جانب الواجهة (UX Validation):**
- **الهدف:** إعطاء ملاحظات فورية للمستخدم
- **القواعد:**
  - ✅ الحقول الإجبارية يجب أن تكون غير فارغة
  - ✅ السعر يجب أن يكون أكبر من 0
  - ✅ المخزون لا يمكن أن يكون سالباً
- **العرض:** رسائل خطأ واضحة بالفرنسية تحت كل حقل

**ب) التحقق من جانب الخادم (Server Validation):**
- **الهدف:** ضمان سلامة البيانات وإتباع قواعد العمل
- **المسؤول:** الخادم (Backend)
- **العرض:** نفس نمط عرض الأخطاء (حقلية أو عامة)

#### 📋 الحقول الإجبارية:
- ✅ **Nom du produit** - اسم المنتج
- ✅ **Marque** - العلامة التجارية
- ✅ **Sous-catégorie** - السُب-فئة
- ✅ **Fournisseur** - المورد
- ✅ **Prix d'achat** - سعر الشراء
- ✅ **Stock initial** - المخزون الأولي

#### 📋 الحقول الاختيارية:
- ⚪ **Catégorie** - الفئة (لتسهيل التصفية)
- ⚪ **Seuil de stock faible** - حد المخزون المنخفض (القيمة الافتراضية: 3)

---

### 4. **حالات التحميل والتغذية الراجعة (Loading States)**

#### ⏳ أثناء التحميل:
- زر "Créer/Enregistrer" يُعطل تلقائياً
- ظهور أيقونة تحميل مع نص "Chargement..."
- منع الإرسال المزدوج

#### ✅ بعد النجاح:
- إعادة التوجيه التلقائي إلى `/dashboard/products`
- رسالة نجاح (إذا كانت هناك حاجة)

#### ❌ عند الخطأ:
- عرض الأخطاء مباشرة تحت الحقول المتأثرة
- رسالة عامة في أعلى النموذج (إذا لزم الأمر)
- إمكانية تصحيح الأخطاء وإعادة المحاولة

---

### 5. **التنقل والوصول (Navigation & Access)**

#### 🔗 طرق الوصول:

**أ) من صفحة قائمة المنتجات:**
- زر واضح **"Nouveau produit"** في رأس الصفحة
- أيقونة `+` مع النص بالفرنسية
- تصميم جذاب باستخدام `variant="primary"`

**ب) من جدول المنتجات:**
- رابط **"Modifier"** لكل منتج في الجدول
- يؤدي مباشرة إلى صفحة التعديل مع البيانات المحملة مسبقاً

**ج) من داخل النموذج:**
- زر **"Annuler"** للرجوع إلى قائمة المنتجات
- زر **"Créer/Enregistrer"** لإرسال النموذج

---

### 6. **إمكانية الوصول (Accessibility)**

#### ♿ المميزات:
- ✅ **Labels مرتبطة:** كل حقل إدخال له `label` مرتبط عبر `htmlFor`
- ✅ **حالات التركيز:** حدود واضحة عند التركيز (focus-visible)
- ✅ **التنقل بالكيبورد:** جميع الحقول قابلة للوصول عبر Tab
- ✅ **رسائل الخطأ القابلة للوصول:** استخدام `role="alert"` في `FormError`
- ✅ **أوصاف إضافية:** `description` للمساعدة في فهم الحقول

---

## 🎨 التصميم البصري (Visual Design)

### 🎨 الألوان والثيمات:
- **ألوان الخطأ:** أحمر (`theme.colors.error`) مع خلفية فاتحة
- **ألوان النجاح:** أخضر (`theme.colors.success`)
- **الحدود:** عند الخطأ تصبح حمراء، عند التركيز تصبح زرقاء
- **الأيقونات:** استخدام `AppIcon` فقط (مثل `warning`, `add`, `edit`)

### 📐 المسافات والتخطيط:
- **مسافات متسقة:** استخدام `theme.spacing` فقط
- **تخطيط متجاوب:** عمود واحد على الموبايل، عمودين على سطح المكتب (للحقول المتجاورة)
- **حدود وارتداءات:** استخدام `theme.borderRadius` و `theme.shadows`

---

## 🔄 سيناريوهات الاستخدام (Use Cases)

### 📝 إنشاء منتج جديد:

```
1. النقر على "Nouveau produit"
   ↓
2. ملء الحقول الإجبارية:
   - Nom: "Samsung TV 32 pouces"
   - Marque: "Samsung"
   - Catégorie: "Électronique"
   - Sous-catégorie: "Téléviseurs" (يُفلتر تلقائياً)
   - Fournisseur: "Fournisseur A"
   - Prix d'achat: 2500
   - Stock initial: 10
   ↓
3. النقر على "Créer le produit"
   ↓
4. التحقق من البيانات (UX)
   ↓
5. إرسال للخادم (API)
   ↓
6. التحقق من البيانات (Server)
   ↓
7. ✅ النجاح → إعادة التوجيه
   ❌ الخطأ → عرض الأخطاء
```

### ✏️ تعديل منتج موجود:

```
1. النقر على "Modifier" في جدول المنتجات
   ↓
2. تحميل بيانات المنتج تلقائياً
   ↓
3. عرض النموذج مع البيانات المحملة
   ↓
4. تعديل الحقول المطلوبة
   ↓
5. النقر على "Enregistrer les modifications"
   ↓
6. نفس عملية التحقق والإرسال
```

---

## 🚀 المميزات الذكية (Smart Features)

### 1. **Pre-filling في وضع التعديل:**
- تحميل تلقائي لجميع بيانات المنتج
- تحويل البيانات المتداخلة (nested) إلى صيغة مسطحة (flat) للنموذج
- مثال: `product.brand.id` → `values.brandId`

### 2. **إعادة تعيين تلقائي:**
- عند تغيير الفئة (Catégorie)، يتم إعادة تعيين السُب-فئة تلقائياً
- يمنع إرسال بيانات غير متسقة

### 3. **منع الإرسال المزدوج:**
- تعطيل الزر أثناء التحميل
- منع إرسال النموذج أكثر من مرة

### 4. **معالجة الأخطاء الشاملة:**
- أخطاء حقلية (field-level)
- أخطاء عامة (global)
- أخطاء الشبكة (network errors)

---

## 📱 التجاوب (Responsive Design)

### 🖥️ سطح المكتب:
- عرض الحقول المتجاورة جنباً إلى جنب (مثل Marque و Catégorie)
- عرض النموذج في مركز الشاشة بعرض محدود (max-width: 800px)

### 📱 الموبايل:
- جميع الحقول في عمود واحد
- أزرار الإجراءات تبقى مرئية ويمكن الوصول إليها
- رأس الصفحة يتكيف (PageHeader)

---

## ✅ قائمة التحقق (Checklist)

قبل اعتبار Task 7.4 مكتمل، تأكد من:

- ✅ جميع الحقول الإجبارية تعمل بشكل صحيح
- ✅ التدرج (cascade) بين Catégorie و Sous-catégorie يعمل
- ✅ رسائل الخطأ تظهر بشكل صحيح وواضح
- ✅ الزر يُعطل أثناء التحميل
- ✅ إعادة التوجيه تعمل بعد النجاح
- ✅ صفحة التعديل تحمل البيانات بشكل صحيح
- ✅ التصميم متجاوب على جميع الأجهزة
- ✅ إمكانية الوصول (accessibility) كاملة

---

## 📚 المراجع

- **التوثيق التقني:** `docs/phases/phase-7/task-7.4-product-form.md`
- **معمارية المشروع:** `docs/design/ARCHITECTURE_BLUEPRINT.md`
- **نظام التصميم:** `styles/theme.js`
- **معايير الترميز:** `docs/standards/CODING_STANDARDS.md`

---

**آخر تحديث:** 2025-01-14  
**الحالة:** ✅ مكتمل

