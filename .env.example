# Store Management System - Environment Variables Example
# Copy this file to .env and fill in your values

# ============================================
# Core Configuration (Required)
# ============================================

# Node.js Environment
# Options: development | production | test
NODE_ENV=development

# Application Environment (Required when NODE_ENV=production)
# Options: staging | production
# Note: Only required for staging/production, not for local development
# APP_ENV=staging

# MongoDB Atlas Connection String
# Format: mongodb+srv://[username]:[password]@[cluster].mongodb.net/[database-name]?retryWrites=true&w=majority
MONGODB_URI=mongodb+srv://user:password@cluster.mongodb.net/store-dev?retryWrites=true&w=majority

# JWT Secret Key
# Must be at least 32 characters long
# Generate with: openssl rand -base64 32
JWT_SECRET=your-secret-key-change-in-production-minimum-32-characters-long

# ============================================
# Optional Configuration
# ============================================

# Skip Authentication (Development Only)
# WARNING: Never enable in production!
# Options: true | false
SKIP_AUTH=true

# JWT Token Expiration
# Format: Time duration string (e.g., "1d", "7d", "24h", "3600s")
# Default: "1d" (1 day)
JWT_EXPIRES_IN=1d

# Public API URL (Optional)
# Used when API URL cannot be auto-detected from request headers
# NEXT_PUBLIC_API_URL=http://localhost:3000/api

# Public Application URL (Optional)
# Used for generating absolute URLs (e.g., in emails, PDFs)
# Auto-detected by Vercel in production
# NEXT_PUBLIC_APP_URL=http://localhost:3000

# ============================================
# Script-Only Variables (For Initial Setup)
# ============================================

# First Manager Account (Only for scripts/create-first-manager.js)
# FIRST_MANAGER_NAME=Admin User
# FIRST_MANAGER_EMAIL=admin@example.com
# FIRST_MANAGER_PASSWORD=secure-password-123
